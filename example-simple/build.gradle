project.ext.sourceCompatibility = JavaVersion.VERSION_1_7

subprojects {
    apply plugin: "java"

    dependencies {
        compile "com.intellij:annotations:12.0"
    }

    test {
        ignoreFailures = true
    }

    repositories {
        mavenCentral()
    }
}